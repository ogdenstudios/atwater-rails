<section class="portfolio__all">
  <% @authors.each do |author| %>
    <% if author.books.count > 0 && author.visible %>
      <%= render 'partials/author', author: author, showTestimonial: false %>
    <% end %>
  <% end %>
</section>
